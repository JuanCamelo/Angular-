export interface CollectionDTO {
    idDTO: string;
    descripcionDTO: string;
    estadoDTO: string;
}

export interface Collection {
    id: string;
    descripcion: string;
    opcional: string;
}